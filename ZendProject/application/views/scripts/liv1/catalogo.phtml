<section id="contenuto_top">
    <?php 
    if (is_null($this->EvSelezionato)){
        echo $this->filtroForm; }
    ?>    
</section>
<section id="contenuto_centrale">
<?php 
       
        if (!is_null($this->EvSelezionato))
        {
            echo $this->AnteprimaEvento($this->eventi,$this->numpart,'singolo');
            $nascosto=false;
            $ruolo=$this->authInfo('Ruolo');
            $utente=$this->authInfo('Username');
            if($ruolo=='utente'){ //modificare per rendere visibile su tutti i livelli tranne liv1 es: !is_null($ruolo)                
                
                foreach($this->partecipazioni as $part){
                    if($part->Utente==$utente)
                        $nascosto=true;                   
                }
               if($nascosto==false){
                echo '<div class="tasto_partecipazione">'.$this->tastopartecipazioneForm.'</div>';}
                
                $evento=$this->eventi;
                if ($evento->Biglietti_Rimanenti==0){
                    echo '<div class="tasto_acquisto"><h3>I biglietti sono terminati</h3></div>';
                }
                else{ //da considerare il fatto che se il current time > data_fine_acquisto questo tasto non di deve vedere, perchè su questa pagina verranno reindirizzati gli utenti dallo storico
                    echo '<div class="tasto_acquisto">'.$this->tastoacquistoForm.'</div>';}
                }   
        }
         else if (!is_null($this->eventi))
        {
            echo '<h1>Lista eventi</h1>';
            echo '<ul id="listaeventi">';
            $count=0;
            foreach ($this->eventi as $evento){
                echo $this->AnteprimaEvento($evento,'anteprima');
                $count++;
            }
            echo '</ul>';
            if($count==0){
                echo '<h2>Non ci sono eventi che corrispondono ai parametri di ricerca</h2>';
            }
        }
        
        
    
        if($this->eventi instanceof Zend_Paginator) {
        echo $this->paginationControl($this->eventi,'Sliding','liv1/_paginatoreventicatalogo.phtml');
        } 
        ?> 
</section>